name: my_first_worlflow

on:
  push:
    branches:
      - main
jobs:
  job_one:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: greeting
        run: echo "Hello Stranger, Welcome to my world"
